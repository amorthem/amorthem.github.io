import React from "react";
import ReactDOM from "react-dom";  // นำเข้า react-dom เข้ามาในตัวแปรชื่อ ReactDOM
import UserList from "./App";




ReactDOM.render(
<UserList />, 
document.getElementById("root")
);
